# Exploring multitask learning for neuroimaging models

### Environment Setup 

# Using pyproject.toml
pip install -e . 

# one time install and manual run of pre commit
pip install pre-commit
pre-commit install
pre-commit run --all-files
